const express = require('express');
const app = express();

const 

app.listen(3000, (err) => { 
    if(err) {
        console.log(err)
    } else {
        console.log('server is running....')
    }
})